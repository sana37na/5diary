import React from "react";

const Header = (props) => {

    return (
        <header>
            <h1 className="page-name">
                <a href='/'>5th Dimension Diary</a></h1>
        </header>
    )
}

export default Header;